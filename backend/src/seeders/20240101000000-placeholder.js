'use strict';

/** Placeholder seeder — no seed data required for initial setup. */
module.exports = {
  async up() {
    // Intentionally empty
  },

  async down() {
    // Intentionally empty
  },
};
